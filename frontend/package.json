{
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "esbuild": "^0.19.4",
    "http-server": "^14.1.1",
    "purescript": "^0.15.11",
    "spago": "^0.21.0"
  },
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "bundle": "spago bundle-app -t ./dist/index.js",
    "serve": "http-server ./dist -p 3000"
  }
}
